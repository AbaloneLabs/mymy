# Agent instructions
AGENTS.md

# Internal documents (local-only)
plans/
docs/
qa/

# Environment files, including secrets
.env
.env.*
!.env.example

# Vendored third-party source (e.g. hermes-agent) for local analysis only.
# Never commit external source trees.
opensource/

# Local-only runtime mounts and scratch files
.local/
data/
api/data/

# Rust build artifacts
api/target/

# Node / bun
web/node_modules/
web/dist/
# Browser-test artifacts are local/CI evidence and may contain rendered test
# content, so they never belong in source control.
web/playwright-report/
web/test-results/
